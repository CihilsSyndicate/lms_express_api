import { Request, Response } from 'express';

export const createSubmaterial = async (_req: Request, res: Response) => {
  res.status(410).json({ message: 'Submateri has been removed. Use Materi API instead.' });
};

export const getSubmaterialsByMaterial = async (_req: Request, res: Response) => {
  res.status(410).json({ message: 'Submateri has been removed. Use Materi API instead.' });
};

export const getSubmaterialDetail = async (_req: Request, res: Response) => {
  res.status(410).json({ message: 'Submateri has been removed. Use Materi API instead.' });
};

export const updateSubmaterial = async (_req: Request, res: Response) => {
  res.status(410).json({ message: 'Submateri has been removed. Use Materi API instead.' });
};

export const deleteSubmaterial = async (_req: Request, res: Response) => {
  res.status(410).json({ message: 'Submateri has been removed. Use Materi API instead.' });
};
